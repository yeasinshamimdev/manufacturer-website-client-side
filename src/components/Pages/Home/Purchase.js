import React from 'react';
import { useParams } from 'react-router-dom';

const Purchase = () => {
    const { _id } = useParams();

    return (
        <div>

        </div>
    );
};

export default Purchase;